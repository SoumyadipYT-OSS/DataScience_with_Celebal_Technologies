# Empty Lists
empty_list = [] 
empty_list = list()

# Empty Tuples
empty_tuple = ()
empty_tuple = tuple()

# Empty Sets
empty_set = {}  # This will create an empty dictionary NOT a set
empty_set = set() # This will create an empty set

'''

In Python, there are different types of collections available for use:

1. Lists: `[]` or `list()`
2. Tuples: `()` or `tuple()`
3. Sets: `{}` or `set()`
4. Dictionaries: `{}` or `dict()`

When creating an empty collection, it's important to choose the appropriate type based on the intended usage. Creating an empty list requires using the square brackets `[]`, while creating an empty tuple requires using the round brackets `()`. Similarly, creating an empty set requires using the curly brackets `{}`, and creating an empty dictionary requires using the `dict()` constructor.

Note: While creating an empty dictionary using `{}`, it's recommended to use a space after the curly braces for better readability and to avoid confusion with the set literal `{}`.
'''
